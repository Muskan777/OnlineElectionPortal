declare function _exports({ dns, ipns, peerId, isOnline, options: { offline } }: {
    dns: import('.').DNS;
    ipns: import('.').IPNS;
    peerId: import('.').PeerId;
    isOnline: import('.').IsOnline;
    options: ResolveOptions;
}): (name: string, options?: (Options & import("ipfs-core-types").AbortOptions) | undefined) => AsyncIterable<string>;
export = _exports;
/**
 * IPFS resolve options.
 */
export type Options = {
    /**
     * - do not use cached entries.
     */
    nocache?: boolean | undefined;
    /**
     * - resolve until the result is not an IPNS name.
     */
    recursive?: boolean | undefined;
};
/**
 * IPFS resolve options.
 */
export type ResolveOptions = {
    offline?: boolean | undefined;
};
/**
 * IPFS resolve options.
 */
export type AbortOptions = import("ipfs-core-types").AbortOptions;
//# sourceMappingURL=resolve.d.ts.map