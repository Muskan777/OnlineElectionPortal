export = HttpApi;
declare class HttpApi {
    constructor(ipfs: any, options?: {});
    _ipfs: any;
    _log: debug.Debugger & {
        error: debug.Debugger;
    };
    /**
     * Starts the IPFS HTTP server
     *
     * @returns {Promise<HttpApi>}
     */
    start(): Promise<HttpApi>;
    _apiServers: any[] | undefined;
    _createApiServer(host: any, port: any, ipfs: any, cors: any): Promise<any>;
    get apiAddr(): multiaddr;
    stop(): Promise<HttpApi>;
}
import debug = require("debug");
import multiaddr = require("multiaddr");
//# sourceMappingURL=index.d.ts.map